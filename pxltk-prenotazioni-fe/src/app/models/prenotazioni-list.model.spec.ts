import { PrenotazioniList } from './prenotazioni-list.model';

describe('PrenotazioniList', () => {
  it('should create an instance', () => {
    expect(new PrenotazioniList()).toBeTruthy();
  });
});
